{
  "name": "multi-stage-ai-support-workflow",
  "version": "1.0.0",
  "private": true,
  "description": "A lab demonstrating a Chat UX to Codex IDE UX workflow with a validated JSON handoff.",
  "type": "module",
  "scripts": {
    "validate:stage1": "node scripts/validate-stage1.mjs handoff/triage.json",
    "stage2": "node scripts/run-stage2.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ajv": "8.17.1"
  }
}
